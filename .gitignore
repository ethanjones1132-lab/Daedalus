# --- Rust / Tauri ---
src-tauri/target/
**/*.rs.bk

# --- Node / Bun / Vite ---
node_modules/
dist/
.vite/
*.tsbuildinfo
bun.lockb

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# --- Recovery debris quarantine (kept out of history) ---
_quarantine/

# --- OS / editor junk ---
.DS_Store
Thumbs.db
*.swp

# --- WSL/Windows blunder artifacts (never commit) ---
nul
DumpStack.log
*.tmp

# --- Local secrets / config ---
.env
.env.*
!.env.example

# --- Local agent session notes (never commit) ---
.hermes/

# --- Claude Code worktrees (never commit) ---
.claude/worktrees/

# --- Codex worktrees (never commit) ---
.worktrees/

# --- Local UI-automation diagnostic scratch (SkyLocal / ad-hoc screenshot helpers) ---
sky-*.jpg
sky-interact.mjs

# --- Conductor terminal session runtime state (PIDs, CWD tracking) ---
terminals/

# --- Python bytecode caches (never commit) ---
__pycache__/
*.pyc
*.pyo

# --- Temporary scripts (never commit) ---
scripts/update-priorities-*.py
scripts/insert-priorities-*.py
scripts/pin-first-token-timeout-telemetry.py
scripts/loosen-claude-delegate-timeout.py
scripts/jarvis-2026-07-21-evening-flake-pin.py
scripts/_inspect-bs.py
scripts/jarvis-2026-07-21-evening-priorities.py
scripts/render-session-stats-pill.py
scripts/fix-session-stats-pill-props.py
scripts/verify-session-stats-feature.py
scripts/prepend-priorities-session-stats.py
scripts/diagnose-priorities.py
scripts/verify-priorities-update.py
scripts/prepend-priorities-sessions-filter.py
scripts/verify-priorities-sessions-filter.py

# --- Live benchmark output ---
scripts/benchmark-tier2b/results-tier2b.json
